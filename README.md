# Liberia Business Credit (Repacked v2)

- Explicit Flask folders: `template_folder="templates"`, `static_folder="static"`
- Includes all required templates.
- Routes: `/`, `/login`, `/report/create`, `/report/<id>`, `/report/<id>/download.pdf`, `/ping`
